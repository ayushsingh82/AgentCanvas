# MongoDB Connection
# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/agent-builder

# For MongoDB Atlas (cloud):
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/agent-builder?retryWrites=true&w=majority

# Agent Base URL (for generating agentChatURL)
# This is the base URL where your NullShot agents will be hosted
AGENT_BASE_URL=https://agent.nullshot.io
